<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>{{pageTitle}}</title>
  </head>
  <body>
    {{> header}}
    <h1>{{pageTitle}}</h1>
    <ul>
      {{#each p in projects}}
        <li>{{p.projectName}}</li>
      {{/each}}
    </ul>
    <p>{{port}}</p>
    {{> footer}}
  </body>
</html>
